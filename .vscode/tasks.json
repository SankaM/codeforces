{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Codeforces: run samples (cf run --all)",
      "type": "shell",
      "command": "\"${workspaceFolder}/bin/cf\" run --all",
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
