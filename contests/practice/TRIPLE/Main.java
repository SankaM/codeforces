import java.io.*;
import java.util.*;

/**
 * Codeforces-compatible single-file submission.
 * - Public class Main
 * - No package declaration (do not add {@code package ...})
 *
 * Problem: contest practice / TRIPLE
 */
public class Main {

    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        PrintWriter out = new PrintWriter(new BufferedOutputStream(System.out));

        // TODO: parse stdin, compute answer, print to out.

        out.flush();
        out.close();
    }
}
